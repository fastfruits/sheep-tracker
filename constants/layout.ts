/**
 * The screens were designed as a phone-width layout. On a desktop browser they
 * would otherwise stretch edge to edge, so content is clamped and centred.
 * On phones this is a no-op — every phone is narrower than the max.
 */
export const CONTENT_MAX_WIDTH = 720;
