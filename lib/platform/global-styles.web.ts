/**
 * Web-only global stylesheet. Metro supports global CSS imports for the web
 * bundle; the native counterpart is an empty module so screens can import this
 * unconditionally.
 */
import '@/styles/web.css';
